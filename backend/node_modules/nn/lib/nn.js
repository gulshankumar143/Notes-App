

module.exports = function (opts) {
    return new Perceptron(opts)
}

function Perceptron (opts) {
    this.opts = opts
}

Perceptron.prototype.train = function (input, output) {
    
}

if (require.main === module) {
     var p = new Perceptron({
        numLayers: 1,
        neuronsPerLayer: 2,
        weights: 0.1,
        activationFn: 'step',
        bias: -1 
     })

     // train it to perform AND
     p.train([0,0], [0])
     // p.train([0,1], [0])
     // p.train([1,0], [0])
     // p.train([1,1], [1])

     // p.test
}